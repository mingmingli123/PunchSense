import { bindKeyboardShortcuts } from "./keyboard_bindings.js?v=auto-workflow-pin-endpoint-final-v1-20260827";
import { bindCanvasControlEvents } from "./control_bindings.js";
import { bindCanvasPointerEvents } from "./pointer_events.js";
import { bindSnakeManagerEvents } from "./snake_manager_bindings.js?v=auto-workflow-pin-endpoint-final-v1-20260827";
import { bindToolbarEvents } from "./toolbar_bindings.js?v=auto-workflow-pin-endpoint-final-v1-20260827";

export function bindInteractionEvents(deps) {
  const {
    canvas,
    svgImport,
    projectImport,
    tpuSnakeList,
    controls,
    state,
    config,
    fit,
    pxToMm,
    polygonBounds,
    normalizeFrame,
    pointInAnyPolygon,
    simplifyPath,
    distancePointToSegment,
    clamp,
    downloadTextFile,
    gcodeFilename,
    projectFilename,
    currentTpuSnakeStats,
    currentTpuSnakePreviewPaths,
    generateGcode,
    exportConfig,
    exportProject,
    importProjectFile,
    importSvgFile,
    draw,
    drawSelectionPreview,
    scheduleSelectionPreview,
    cancelScheduledSelectionPreview,
    lockCanvasDrag,
    unlockCanvasDrag,
    preventPageScrollDuringCanvasDrag,
    pushUndoSnapshot,
    undoLastOperation,
    copySelectedShapes,
    pasteCopiedShapes,
    assignMaterialToSelection,
    assignTpuFillModeToSelection,
    assignTpuFillModeToSelectionOrAllT0,
    cleanupRegionOverrides,
    setSelectedShapes,
    selectSingleShape,
    toggleSelectedShape,
    selectedShapeIndices,
    syncMaterialSelectToPrimary,
    selectRegionAtPoint,
    nearestShapeHandle,
    hitShape,
    nearestVertex,
    pickTpuSnakeEndpoint,
    startTpuSnakeEndpointPicking,
    openShapeSizeEditor,
    clearShapes,
    clearTpuSnake,
    setSvgImportStatus,
    createRectShape,
    createRectShapeFromDrag,
    createBasicShapeFromEdgeDrag,
    updateShapeFromHandle,
    moveShapeByPointer,
    armShapePlacement,
    placePcbCutout,
    rotatePcbCutout,
    snakeTargetDrawDelayMs,
    numericDrawDelayMs,
  } = deps;

  const {
    scheduleNumericDraw,
    cancelScheduledNumericDraw,
  } = bindCanvasControlEvents({
    controls,
    draw,
    assignMaterialToSelection,
    assignTpuFillModeToSelectionOrAllT0,
    numericDrawDelayMs,
  });

  bindCanvasPointerEvents({
    canvas,
    controls,
    state,
    config,
    fit,
    pxToMm,
    polygonBounds,
    normalizeFrame,
    pointInAnyPolygon,
    simplifyPath,
    distancePointToSegment,
    clamp,
    currentTpuSnakePreviewPaths,
    draw,
    drawSelectionPreview,
    scheduleSelectionPreview,
    cancelScheduledSelectionPreview,
    lockCanvasDrag,
    unlockCanvasDrag,
    preventPageScrollDuringCanvasDrag,
    pushUndoSnapshot,
    setSelectedShapes,
    selectSingleShape,
    toggleSelectedShape,
    selectedShapeIndices,
    selectRegionAtPoint,
    nearestShapeHandle,
    hitShape,
    nearestVertex,
    pickTpuSnakeEndpoint,
    startTpuSnakeEndpointPicking,
    openShapeSizeEditor,
    clearShapes,
    createRectShapeFromDrag,
    createBasicShapeFromEdgeDrag,
    updateShapeFromHandle,
    moveShapeByPointer,
  });

  bindKeyboardShortcuts({
    state,
    undoLastOperation,
    copySelectedShapes,
    pasteCopiedShapes,
    assignMaterialToSelection,
    assignTpuFillModeToSelection,
    cleanupRegionOverrides,
    setSelectedShapes,
    selectedShapeIndices,
    pushUndoSnapshot,
    draw,
  });

  bindToolbarEvents({
    svgImport,
    projectImport,
    controls,
    state,
    config,
    downloadTextFile,
    gcodeFilename,
    projectFilename,
    currentTpuSnakeStats,
    generateGcode,
    exportConfig,
    exportProject,
    importProjectFile,
    importSvgFile,
    draw,
    pushUndoSnapshot,
    selectSingleShape,
    clearShapes,
    clearTpuSnake,
    setSvgImportStatus,
    createRectShape,
    armShapePlacement,
    placePcbCutout,
    rotatePcbCutout,
  });

  bindSnakeManagerEvents({
    tpuSnakeList,
    state,
    draw,
    scheduleNumericDraw,
    cancelScheduledNumericDraw,
    startTpuSnakeEndpointPicking,
    clearTpuSnake,
    snakeTargetDrawDelayMs,
  });
  window.addEventListener("resize", draw);
}
